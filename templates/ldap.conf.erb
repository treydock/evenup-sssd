# Managed by puppet - do not modify

# See ldap.conf(5) for details
# This file should be world readable but not world writable.

#SIZELIMIT	12
#TIMELIMIT	15
#DEREF		never

URI <%= scope.lookupvar('sssd::ldap_uri_array').join(' ') %>
BASE <%= scope.lookupvar('sssd::ldap_base') %>
TLS_CACERT <%= scope.lookupvar('sssd::ldap_tls_cacert_real') %>
TLS_REQCERT <%= scope.lookupvar('sssd::ldap_tls_reqcert') %>
